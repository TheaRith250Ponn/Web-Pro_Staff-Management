To run this project:
1. Make sure we have SQL database running, and import "staff_management.sql"
2. Go to project folder run: npm install (to install all dependencies packages)
3. Start project: node app.js or nodemon app.js


Fix the port number if you cannot run.